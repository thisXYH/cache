{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test cache provider",
            "type": "shell",
            "command": "echo \"test start\"",
            "dependsOrder": "sequence",
            "dependsOn":["test cache clearn", "test cache provider core"]
        },
        {
            "label": "test cache clearn",
            "type": "shell",
            "command": "go clean -testcache"
        },
        {
            "label": "test cache provider core",
            "type": "shell",
            "command": "go test -timeout 30s -v -cover -run \"^(TestRedisCacheProvider_Order|TestMemoryCacheProvider_Order)$\" cache"
        },
    ]
}